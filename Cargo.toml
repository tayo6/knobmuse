[package]
name = "gradient-knob"
version = "0.1.0"
edition = "2021"
 
[dependencies]
eframe = { version = "0.27" }
egui = "0.27"
# No web-sys needed - eframe re-exports what you need
# Haptics replaced by visual pulse